# Fiche de lecture 


## Main Takeway : 
- OS's has their own caches , e.g. `TLB` 
- "although the OS's caches such as the TLB don't need to be changed, tasks are likely to operate on different parts of memory, so data required by the newly scheduled task may not be in a memory cache." increase memory allocation , copying overhead !
- `Pararllelism` is a `Resource` ;
- `async tasks` aren't magic solution ( one fit all solution  ) , apps require both ! 
- Increasing the concurrency of a system without increasing parallelism can never make it faster (although it can make the system more responsive and it may make it feasible to implement optimizations which would otherwise be impractical).
- `Tokio runtime` which primarily supports the last model. I.e., the behavior regarding parallelism is similar to concurrency with threads.
- should i use this `Runtime`feature to avoid the blocking ? 

## Questions : 
- from "To get any work done they must be driven forward by an external force (usually an async runtime)." cant we get async to work without other frameworks ? 


## Definitions : 
- what's   `TLB` ( cache ) : 
> 

