# Fiche


## Main Takeaway ( Plan ):
### Proposal 1 : 
- spawn a task (`Ui` process ) that takes does :
  - create a (`tx`, `rx`) , clone the `tx` and give it to the broker , move the `rx` into the `Ui` process   
  - comm with broker , gets the `buffers` ( text content ) from there 
  - comm with broker , gets the `active buffer` 
  - the `active buffer`is the one who is gonna be polled for inputs 
#### Notes on proposal ( challenges ): 
- i still don't know how to make the boundaries clear between to rendered buffers
- should i only render buffers based on some layout model ? e.g. ( 1 left , 1 right ) or ( 1 left , 2 right )

